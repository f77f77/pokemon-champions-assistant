# Deprecated: name-keyed per-species PNGs

Team Preview matching no longer loads `public/templates/{englishName}.png`.

Use `public/sprites/sprite_poke.png` + `atlas.json` (nationalDex keys, in-memory crops).

`manifest.json` is kept only as a pointer at the sheet atlas.
